image: r8.im/ani0135/chatbot  # 👈 customize this

build:
  gpu: true
  # remote: true
  python_version: "3.11"
  python_packages:
    - torch
    - transformers
    - accelerate
    # - sentencepiece
    # - bitsandbytes  # Optional if needed for model
    - huggingface-hub

predict: "predict.py:Predictor"
